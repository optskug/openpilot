# TSK Manager

## Background

comma.ai makes DIY ADAS devices — currently the comma threeX (C3X / tizi) and comma four (C4 / mici). Toyota added cryptographic SecOC signatures to CAN messages, blocking comma from writing to the bus.

Willem (ex-comma) found a way to pull the SecOC key from the EPS firmware on 2021–23 RAV4 Prime; the same hack works on 2021–23 Sienna Hybrid, and with modifications on Yaris. TSKM is Calvin's GUI around Willem's method. Background in `/Users/calvin/GitRepos/docs/README.md`.

Calvin's cars: 2023 Sienna Hybrid (C3X), 2023 Bolt EV 2LT (C4). Both devices validate against the Sienna.

## Architecture

The `tskm` branch, based on `commaai/nightly-dev`, is a local phone-first web app on port `11111`. It replaced the RayLib GUI, whose final release is frozen at `tskm-0.11.0`. The web app was developed on a `tskmloop` branch, since folded into `tskm` and deleted.

### Boot Flow

`launch_chffrplus.sh` on device boot:

1. Creates `/cache/tsk` **and `/cache/params`** and chowns both to comma — the server runs as comma but `/cache` is root-owned, so the dirs it writes to must exist first: `/cache/tsk` for dumps and the oracle, `/cache/params` for the installed `SecOCKey`. Recreated each boot; `/cache` clears on AGNOS update.
2. Runs `python3 tsk/prefetch.py` — standalone RayLib GUI that clones the recommended/alternate openpilot branches. Blocks until done.
3. Starts `tsk.web.server` in the background, *before* the manager, so it survives the pipeline's `pkill manager.py`.
4. Starts the openpilot manager (`./manager.py`) from the nested `openpilot/` tree.
5. UI at `http://<device-ip>:11111`.

`/data/openpilot` (the `tskm` checkout) is a **wrapper**: `tsk/` (this app) plus a nested `openpilot/` (a full bundled openpilot the device drives with). Installing `recommended`/`alternate` `rmtree`s + `move`s `/data/openpilot`, replacing the wrapper with a plain openpilot; `RebootManager.execute()` kills the manager first (`_stop_manager()`) so it isn't running from the tree being moved. "Install a different fork/branch" deletes `/data/continue.sh` (the AGNOS boot pointer, `cd /data/openpilot && exec ./launch_openpilot.sh`), dropping the device to the comma installer.

### File Layout

- `tsk/prefetch.py` — standalone RayLib prefetch GUI (not manager-managed). Detects C3X vs C4 screen size.
- `tsk/lib/` — all TSKM behavior; shared logic lives here.
  - `env.py` — paths, branch constants, device detection: `is_agnos`, `CACHE_DIR` (`/cache` on device, `~/comma_data/cache` off), `DATAFLASH_DIR`, `DATAFLASH_PAYLOAD_PATH`, `CAN_MESSAGES_DIR`, `CAN_ORACLE_PATH`, and the recommended (`commaai/nightly-dev`) / alternate (`sunnypilot/staging`) install targets.
  - `extractor.py` — legacy single-step extraction (Willem). `hack()` uploads `payload.bin`, dumps RAM `0xFEBE6E34–0xFEBE6FF4`, parses KEY_4. `_connect_panda()` stashes the handle; `_close_panda()` releases it from the server's `finally`. pandad flashes the panda firmware on boot, so there's no flash call here.
  - `dump_dataflash.py` — DataFlash dump (the 2021+ path). `dump(progress_cb)` uploads `payload_dataflash_ff200000_ff208000.bin` and dumps `0xFF200000–0xFF208000` (32 KB), returning `{status, frames, bytes, total, dump_path, message}`. `_finalize()` (pure, off-device testable) classifies the outcome:
    - **complete** — full 32768 bytes; writes `dump_ff200000_ff208000.bin`.
    - **partial** — the key window `KNOWN_KEY_OFFSET` (0x6e14) is captured but not the full range; writes the `.partial` sidecar Find can use.
    - **key_missed** — the key window wasn't captured (one frame or nearly all of it); writes nothing, asks for a re-dump.

    `_finalize()` returns the three statuses above; the server's job thread sets `failed` on an unhandled exception. `is_agnos`-gated. Shares the UDS session preamble with `extractor.py` by deliberate duplication (distinct payload, range, and parser, kept independently testable).
  - `collect_can.py` — CAN oracle capture. `collect(progress_cb)` records sync (0x0F) + protected (0x2E4/0x131/0x344) frames on buses 0/2 in READY Mode until both targets are met (sync is the bottleneck; protected floods), capped at 60 s; writes `can_oracle.ndjson`; returns `{status, sync, protected, ...}` (complete | insufficient; the server sets `failed` on an unhandled exception). `count_oracle_frames()` tallies a persisted oracle, skipping malformed lines. `is_agnos`-gated.
  - `matcher.py` — key finder. `run()` reads the oracle + the dump (complete file, else the `.partial` sidecar, setting `dump_partial`) and calls `find_key()`: exhaustive stride-1 scan over every window, 5-sample sync union first pass, accept at ≥ `MATCH_FLOOR` (30) matches with ≥ 2 sync. Hand-rolled RFC-4493 AES-CMAC that computes the same 28-bit SecOC MAC as opendbc `secoc.py` (opendbc uses the pycryptodome `CMAC` library; the MAC values match, the code is a reimplementation). Pure computation; returns the key, does not install it.
  - `key_file_manager.py` — key read/write/delete, `format_key()`.
  - `reboot_manager.py` — reboot/install actions with key-status prompts.
  - `payload.bin` — legacy extraction payload.
  - `payload_dataflash_ff200000_ff208000.bin` — DataFlash dump payload (SHA256 `d48988366b…a06e34`, verified before use; byte-identical to Willem's I-CAN-hack/secoc `while-loop` branch).
- `tsk/web/` — HTTP + static UI only.
  - `server.py` — Python stdlib `ThreadingHTTPServer` on `11111`.
  - `static/index.html` — phone-first main page (iOS Settings design).
  - `static/extractor.html` — legacy TSK Extractor page (dark terminal, auto-runs on load).
  - `static/can-collector.html` — CAN page; POSTs to start the real job, polls `/api/can-status`. Short-circuits a complete oracle, attaches to a running one, retries after insufficient/failed.
  - `static/dataflash-collector.html` — DataFlash page; same pattern against `/api/dataflash-status`.

`tsk/common`, `tsk/c3`, `tsk/c4` should not exist on `tskm`. They are the RayLib layout, preserved at `tskm-0.11.0`.

### Web Server

Binds `0.0.0.0:11111`. Serves frozen assets from `tsk/web/static`. Writes the device URL into `Offroad_NoFirmware` (via the background `offroad_alert_loop`) so the comma screen shows it — rewritten when the URL changes or the file is gone, since the manager wipes it on start.

API endpoints:

- `/api/health` (GET) — server info, dry_run flag, detected addresses.
- `/api/status` (GET) — key install status via `RebootManager.key_status_payload()`.
- `/api/reboot` (GET/POST) — GET lists reboot actions, POST executes one.
- `/api/extract` (POST) — legacy single-step extraction. Off-AGNOS dry run cycles 3 scenarios.
- `/api/uninstall` (POST) — removes the installed key via `KeyFileManager`.
- `/api/can-status` (GET) — `{ready, status, sync_count, protected_count, seconds, message}` from `can_state`. `ready == (status == "complete")`.
- `/api/can-collect` (POST) — starts the background collect job, returns immediately. 409 if a panda op is already running. Off-AGNOS: mock ramp.
- `/api/dataflash-status` (GET) — `{ready, status, frames, bytes, total, message, size}` from `df_state`. `partial` is a key-region-covered dump Find accepts; `key_missed` writes no file (re-dump).
- `/api/dataflash-dump` (POST) — starts the background dump job, returns immediately. 409 if a dump is in progress. Off-AGNOS: `dump()` raises `NotAGNOSError` and the job falls back to a mock ramp.
- `/api/match` (POST) — runs `matcher.run()`; on `found` installs the key via `KeyFileManager` and returns it with a screenshot message (modal title "Success!"), for a complete or partial recovery alike. Otherwise returns status + counts + debug fields (`windows_scanned`, `survivors`, best-candidate `address`, `dump_partial`) for the not-found modal. 500 with traceback on unhandled exception. 409 if a match is already running.
- `/api/clear-cache` (POST) — `clear_can()` deletes the CAN oracle file and `clear_dataflash()` deletes the dump + `.partial` (each also resets its in-memory state). 409 while a dump/collect runs. Does not touch the key.

CAN and DataFlash run as real background jobs: `can_state`/`can_lock` and `df_state`/`df_lock` hold live progress; threads run `collect()` / `dump()`, set `status="failed"` on any unhandled exception, and `start_*_job()` reject a concurrent start. A single `panda_lock` serializes extract/dump/collect over the one physical panda — held for the whole operation and released in the job's `finally`, which also calls `TSKExtractor._close_panda()`. `rehydrate_can_state()` and `rehydrate_dataflash_state()` run in `main()` so persisted CAN/dump data shows as done after a restart.

### Hard Rules

`tsk/web/server.py` is HTTP routing only. Do not add key paths, key validation, key read/write, device detection, extractor wrappers, or reboot file ops. If behavior exists in `tsk/lib`, call it; if it doesn't, add it to `tsk/lib` or ask first. Do not invent alternate key storage — use `KeyFileManager`.

### Storage

- Frozen code: `/data/openpilot/tsk`
- CAN oracle: `/cache/tsk/can-messages/can_oracle.ndjson`
- DataFlash dumps: `/cache/tsk/dataflash/` — `dump_ff200000_ff208000.bin` (exactly 32768 bytes; the matcher prefers it) or a `.partial` sidecar (key window captured, full range not)
- Prefetched repos: `/data/tsk-recommended`, `/data/tsk-alternate`

All of `/cache` survives reboot and clears on AGNOS update.

### UI State

`index.html` has two extraction sections:

**"2021, 2022, 2023 RAV4 Prime & Sienna"** — single TSK Extractor link (`extractor.html`), the legacy single-step RAM extraction. Kept as-is.

**"2021+"** — the three-step pipeline (CAN → DataFlash → Find), all real:

- **CAN row** — detail line `Sync N/50   Protected N/30`; links to `can-collector.html`.
- **DataFlash row** — dot green on complete, **yellow** (`prereq-dot warn`) on a key-region-covered `partial`, red otherwise. `setDfDetail()` shows gray `Dumping N/total` while running, orange `partial`, red `key_missed`; hidden when idle/complete/failed (a failure shows the red dot alone — the error already appeared on the dump page). Links to `dataflash-collector.html`.
- **Find Toyota Security Key** — disabled with "(need CAN)" / "(need DataFlash)" / "(need CAN & DataFlash)" until CAN is collected and a dump exists (complete **or** key-region `partial`). Running shows a darkened overlay + spinner ("Finding key…", `showFinding()`); `updateExtraction()` early-returns while `state.running` so the 1 s poll can't clobber the run. Success → "Success!" modal; complete-dump miss → debug block (best candidate address/matches, windows scanned, bytes) + the #toyota-security report line; partial miss → "Key not found in the partial DataFlash dump."
- **Clear extraction cache** — red when CAN or DataFlash data exists, gray otherwise; resets both via `/api/clear-cache`.
- **Uninstall key** — red when a key is installed, gray otherwise.

CAN targets: 50 sync, 30 protected — a **total** across 0x2E4/0x131/0x344, not gated per-address. Collection stops when both are met (cap 60 s); the progress bar tracks target completion, not elapsed time.

### Method & Design Notes

Durable rationale for why the pipeline is shaped this way:

- **Car-agnostic — no model gate.** Any car the owner picks may run. The verifier is the safety net: a wrong car either fails security access (EPS not in the Willem family) or nothing verifies. A wrong 16-byte window clears a 28-bit sync MAC at 2⁻²⁸; with ≥ 2 sync samples a false install is 2⁻⁵⁶ — no bad install is possible. The only car-specific constant is `KNOWN_KEY_OFFSET = 0x6e14`, used only to classify a partial as usable vs `key_missed`.
- **No candidate system.** Exhaustive stride-1 scan over every window (~32,753), one AES-CMAC each, sub-second — no entropy filtering, scoring, or caps. Survivors (expect 0–1) get full sync + protected verification. Acceptance is an absolute floor (`MATCH_FLOOR` 30, ≥ 2 sync), not a percentage: a wrong window reaching 30 is ~2⁻⁶⁶⁰.
- **Three failure modes, kept distinct.** (1) Security access denied → EPS not in the exploit family. (2) Dump completes, nothing verifies → exploit works but the key isn't in `0xFF200000–0xFF208000` for this EPS. (3) Verify passes → key extracted.
- **Key delivery.** `KeyFileManager` writes `/cache/params/SecOCKey` + Params `SecOCKey`; `card.py` reads both, gated on `CP.secOcRequired`, surviving the install `move`/reboot/AGNOS. Cars past their last-supported year need SunnyPilot with manual year selection (e.g. 2024 Sienna → pick 2023).
- **Cold dumps often come back `partial`/`key_missed`; the variable is not the car.** The EPS is ruled out categorically: it's a state machine, so after `bl_reset` it returns to the same state and can't be the run-to-run difference. The lost data is a fixed-size (124-byte), moving, single-burst gap during the 8192-frame flood — dropped on the RX/USB path, so by elimination the variable is host/panda-side; which specific state (USB enumeration, RX FIFO, flash-on-boot settle) is unresolved, and "warm-up" is a placeholder for it, not a mechanism. Behavioral remedy (Calvin, in-car): keep the comma powered so the panda doesn't cold-cycle between car power cycles, and the first dump on entering Not Ready is complete every time. Bk2ol's "prime the EPS" and a throwaway retry both worked only by incidentally reaching that warm state — the EPS was never primed. Production keeps the dump as-is; a stuck user's fix is keeping the comma powered, and most Sienna partials still capture the key regardless.
- **Extraction (RAM) vs dump (DataFlash).** The legacy extraction reads a firmware-specific RAM copy at `0xFEBE6E34` (per-car; fails the KEY_4 checksum on Yaris — "needs modifications"). The dump reads persistent DataFlash at `0xFF206E14`, whose layout is shared across these EPS variants — so it generalizes where the RAM extraction needs per-car addresses.
- **DataFlash size.** The dump window is 32 KB; total DataFlash size is unverified (no RH850 part number). Every key seen is inside the window (Sienna/Yaris `0x6e14`; a `0x6410` candidate on the 2024-Sienna profile). A wider-range payload is ready-if-needed, not needed yet.

## Development

### Laptop (macOS)

Direct server test:

```
python3 -m tsk.web.server
```

Off-AGNOS: `/api/extract` dry-run cycles 3 scenarios (fake-key success, short error, long error with traceback). `dump()` / `collect()` raise `NotAGNOSError`, so the background jobs fall back to a **server-side mock ramp**; the collector pages poll the real status endpoints either way.

## Reference

- **Why rebase on nightly-dev** — AGNOS updates are ~1 GB / ~10 min; rebasing keeps TSKM current so users don't hit one mid-extraction. The web-app direction also cuts dependence on comma's brittle RayLib libs.
- **comma release branches** — C3X `release-tizi`, C4 `release-mici`, C3 `release-tici` (discontinued), `release3` (legacy).
- **Versioned TSKM branches** — each release tagged as a branch (e.g. `tskm-0.10.4`); frozen fallbacks, but comma changes can break them. `tskm-0.11.0` is the last RayLib release and the end of that line.
- **Branch naming** — `tskm` is the live web-app branch and the only active one. It carried the RayLib GUI until 2026-07, so journal entries dated before then use `tskm` in the RayLib sense; that code is preserved at `tskm-0.11.0`. `tskmloop`, the branch the web app was built on, was folded into `tskm` and deleted. `span` is the closed Corolla diagnostic fork, pushed for public reading.
- **SSH** — tmux detach on comma devices: backtick, then `d`.

---

## Journal

When the user says "update the journal", add a dated entry summarizing the session.

- **2025-11** — TSKM v0.10.4 shipped on the RayLib `tskm` branch.
- **2026-04** — Fixed `CAN packet version mismatch` (stale panda firmware, killed before pandad could flash) with `flash_panda()` GPIO/DFU recovery, after a bare `panda.flash()` silently failed to persist on one DEV-firmware panda. Multiple users extracted successfully on the RayLib `tskm` branch. (Later superseded by pandad flashing on boot — no flash call in `hack()` now.)
- **2026-07-02 → 07-08** — Built the `tskmloop` web app off `commaai/nightly-dev`: moved shared logic into `tsk/lib`, stdlib server on `11111`, phone-first `index.html`. Replaced the single-step extractor with the three-step **CAN → DataFlash → Find** pipeline; built `collect_can.py`, `dump_dataflash.py`, `matcher.py` and their real background jobs (`panda_lock` serialization, `rehydrate_*` on restart, `_close_panda`). Audited installer → boot → install → key-load end to end.
- **2026-07-08 (in-car)** — First successful Sienna extraction end to end (key `f220…7098` at `0xff206e14`). The partial-dump path also recovered the key in-car. Added the key-region gate (`.partial` only when `0x6e14` is captured, else `key_missed`) and CAN early-stop (stop when both targets are met, not a fixed 60 s).
- **2026-07-09 (prime, not time)** — Controlled in-car experiment: both prime variants dumped 32768/32768 (0 gaps, 2/2); time-only (60 s, no prime) partialed both times. The prime fixes cold partials; waiting does not. The gap is a fixed-size (124-byte) moving single burst → RX/USB-side, not structural (overturns the earlier "structural window" read). Decision (Calvin): keep the dump as-is; tell stuck users to run the Extractor first (manual prime), since the behavior may be Sienna-specific and most Sienna partials still capture the key.
- **2026-07-09 (payload provenance)** — Traced the dataflash path to Willem's I-CAN-hack/secoc `while-loop` branch: the shipped payload is byte-identical to his `payload.bin` (same SHA256), and the driver, shellcode, and matcher are his. No third-party ("Bk2ol") code is used; the one non-Willem trace is the dump's session-flow timing (sleeps 0.5/0.7/1.0 + a `PROGRAMMING → PROGRAMMING` repeat) that differs from the extraction flow, and its origin isn't verifiable from the repos.
- **2026-07-19 (not the car — host/panda-side)** — Corrects the 2026-07-09 "prime, not time" reading. The EPS can't be the run-to-run variable: it's a state machine, so after `bl_reset` it returns to the same state and a second identical dump would behave identically. "Priming the EPS" (Bk2ol's framing, which had propagated into the `dump_dataflash.py` docstring and the Method note) is a category error — a state machine has no warm-up. Calvin's in-car finding: keep the comma powered so the panda doesn't cold-cycle between car power cycles, and the first dump on entering Not Ready comes back complete every time. That puts the run-to-run variable on the host/panda side; the earlier "prime" and "retry" remedies both worked only by incidentally reaching that state. Still behavioral — the specific host-side cause (USB enumeration / RX FIFO / flash-on-boot settle) is unproven, and "warm-up" is a placeholder, not a mechanism.

- **2026-07-23 (2025 Corolla Hybrid — diagnostic session, `span` branch)** — First non-Sienna in-car investigation. Built three read-mostly diagnostic tools on a new `span` branch (a diagnostic fork of tskmloop for the Corolla owner "Spanconstant5"), reached the Corolla EPS, pulled its part number, and hit a programming-session wall. All three tools share `panda_lock`, an off-AGNOS mock, and an **EPS-bus sweep** (`CANDIDATE_BUSES = [0,1,2]`; probe DEFAULT session on each, run on the first the EPS answers on).

  **Tools built (span, commits `d7f196`→`6aca19`):**
  - `tsk/lib/sniff_can.py` + `static/can-sniff.html` + `/api/can-sniff` — read-only all-bus tally: per-bus frame count + distinct arb IDs, plus the SecOC markers (0x0F sync, 0x2E4/0x131/0x344). No payloads recorded. `summarize_counts()` is pure/off-device-testable and shared with the server mock.
  - `tsk/lib/dump_diag.py` + `static/dataflash-diag.html` + `/api/dataflash-diag` — instrumented dump: sweep to the EPS bus, read identity DIDs (0xF181/0xF187/0xF18C/…), run session/security/upload/collect with per-step NRC + full traceback. Runs the real dump if security passes (`DIAG_COLLECT_SECONDS = 30`).
  - `tsk/lib/prog_probe.py` + `static/prog-probe.html` + `/api/prog-probe` — five PROGRAMMING-entry sequences (patient 3 s, double-with-1 s-settle, direct, tester-present, security-first-last), each reset to DEFAULT, recording accepted/NRC/timeout + the security seed/key result. Security last (failed-key lockout risk).
  - `launch_chffrplus.sh` on `span` skips `tsk/prefetch.py` (diagnostic build, no install buttons).

  **Findings (EPS `8965F1208000`):**
  - **Bus topology:** bus 0 == bus 2 (identical 22-ID powertrain, harness-bridged); the EPS + SecOC ride **bus 1**. In READY, bus 1 = 147 IDs + sync 0x0F; in the quieter state it collapses to ~27 IDs, no sync (SecOC signs only in READY, like the Sienna). Sync is 0x0F (same as Sienna); the Sienna's protected IDs 0x2E4/0x131/0x344 are **absent on every bus** — the Corolla signs different, still-unidentified IDs.
  - **EPS reached on bus 1 in software** — the sweep finds it; no physical repin needed *for extraction*. The production dump (`dump_dataflash.py`) still hardcodes bus 0 (`BUS`, lines 161/209/226) and never reaches it — needs repointing (UdsClient + trigger + collect) to bus 1 for a real Corolla dump. `UdsClient` RX is bus-locked (`bus == self.bus`, opendbc `uds.py:364`), so reads come off the same bus writes go out on — shown by DEFAULT/EXTENDED + identity responses arriving on bus 1.
  - **EPS identity (bus 1):** app_sw_id (0xF181) = **8965F1208000**; ecu_serial (0xF18C) = `8965012N50E12H030731`. The 8965F12 family — distinct from Sienna 8965B45 / RAV4 Prime 8965B42. Other identity DIDs → NRC 0x31. This satisfies the long-open "EPS app-string capture every run" item (now in `dump_diag`'s identity sweep).
  - **The wall — programming session.** On bus 1: DEFAULT ok, EXTENDED ok, but `diagnostic_session_control(PROGRAMMING)` **times out** (no response) across all five entry sequences. Security REQUEST_SEED in extended → **NRC 0x7e** (sub-function not supported in active session): the EPS *responds* to security (NRC) but is *silent* on programming. That asymmetry rules out "EPS asleep / wrong bus" — it's alive on bus 1; only PROGRAMMING produces silence.
  - **Reading:** PROGRAMMING likely triggers a bootloader jump / state change whose response reroutes off bus 1 (yc's hypothesis), or is dropped by a gateway. Security is gated behind PROGRAMMING (the 0x7e) — circular, since Willem runs security *inside* the programming session on the Sienna.
  - **Panda:** `DEV-e1b3ecb5-DEBUG` firmware — the nightly-dev DEBUG build stamp (Calvin), benign; RX works (10k frames sniffed).

  **Facts established this session:** serial ≠ key (no crypto link; the SecOC key is re-keyable, the serial is fixed — docs README:1321). Extraction adapts the bus in software; *driving* the Corolla would need a physical repin OR a fork bus-patch (a production fork has fixed, safety-critical bus assignments) — separate, downstream, unverified. NRtD is the right mode for programming (reflash is safety-gated; a live/READY EPS won't grant it); READY is for oracle capture (SecOC signs there). NRC = Negative Response Code (ISO 14229): a response (`0x7F` + service + code), vs a timeout = silence.

  **Next steps (deferred to next in-car session):** (1) Two cheap software tests before the firmware-dump path — **all-bus-listen** after the PROGRAMMING request (catch a response rerouted off bus 1), and **"did-it-take"** (send PROGRAMMING, ignore the timeout, read ACTIVE_DIAGNOSTIC_SESSION 0xF186 to see if the EPS switched silently). One probe covers both. (2) If both dry, the firmware-dump path — buy a bench `8965F1208000` EPS (a China ~$30 part reportedly exists), glitch + dump it as 3b1b.eth did the Sienna (docs README:1662–1665), reverse the programming-entry / payload-signing secret. (3) Repoint the production dump to bus 1 before any Corolla dump (shellcode outputs on the EPS's CAN channel = bus 1). (4) Identify the Corolla's SecOC-signed IDs via a full-payload bus-1 READY capture (needed for both the matcher oracle and a driving port).

- **2026-07-24 (Corolla `span` — security level 0x03 seed in extended; the wall reframed)** — Shipped the no-pin-swap software-probe set (commit `d6526664f2`, on top of `6aca19`); Spanconstant ran it in-car ~10:18–10:22 (real panda `DEV-e1b3ecb5-DEBUG`, EPS on bus 1). One result moves the target.

  **Shipped (`d6526664f2`, 12 files):**
  - `read_mem.py` + `read-mem.html` + `/api/read-mem` — ReadMemoryByAddress (0x23) at the key region + two controls, extended and default.
  - `ident_map.py` + `ident-map.html` + `/api/ident-map` — full identity block (0xF180–0xF195 + VIN) + read-only service-surface map (safe services only: 0x10/0x22/0x23/0x27/0x3e/0x19; no reset/write/routine/download).
  - `reset_probe.py` + `reset-probe.html` + `/api/reset-probe` — hard reset then hammer PROGRAMMING through the reboot; reads 0xF186 after.
  - `prog_probe.py` extended: **did-it-take** (0xF186 before/after a silent PROGRAMMING), **all-bus-listen** (PROGRAMMING physical + functional 0x7df, then raw `can_recv` on every bus for the 0x7a9 response), **security-level sweep** (REQUEST_SEED 0x01..0x0b, no key), **safety-system session 0x04**; SEND_KEY still last.
  - `sniff_can.py` extended: per-bus max DLC + `fd_buses`. `index.html`: production TSKM surfaces CSS-hidden (`span-hidden`; nodes kept so the polling JS / reboot handlers survive); diagnostics regrouped under **Not Ready To Drive** / **READY** headers.

  **In-car findings:**
  - **Security level 0x03 returns a seed in EXTENDED — the headline.** Sweep: `0x01` → NRC 0x7e (exists, wrong session — Willem's Sienna level); **`0x03` → seed `da2df2eff64d95f5426bf3af70bb49aa`**; `0x05/07/09/0b` → NRC 0x12 (don't exist). The Corolla's usable security level is **0x03/0x04**, seed available in extended — **no programming session needed for the seed.** This breaks the 07-23 "circular gate" for level 0x03 (its seed no longer requires entering programming). ~~The blocker moves from "how to enter programming" to "what's the 0x03 seed→key formula."~~ *(Superseded 07-24 evening: level 0x01 — Willem's level, the one gated behind PROGRAMMING — is the level that matters, so the blocker never moved off PROGRAMMING. 0x03 being available in extended argues it is a lesser privilege.)*
  - **0x23 ReadMemoryByAddress is supported and returns NRC 0x31 (request-out-of-range), NOT 0x33 (denied)** at the Sienna addresses (0xFF206E14 / 0xFF200000 / 0xFEBF0000) in extended; NRC 0x7f (not in this session) in default. So 0x23 isn't security-refused — the Corolla's key lives at a *different address* (RH850 part differs). 0x23 is a live read primitive; post-security the range may widen. Potential path: security(0x03) → 0x23 at the right address, skipping programming/upload/trigger entirely.
  - **All three buses are CAN-FD** (bus 0/2 = 64-byte, bus 1 = 32-byte). TSS3 with FD (yc's hypothesis). Classic-CAN ISO-TP still answers session/identity/security-seed on bus 1, so FD framing isn't blocking those. Untested cheap angle: whether PROGRAMMING specifically needs an FD-framed request.
  - **Programming: refused, not rerouted (overturns the 07-23 reroute lean).** did-it-take read session `0x03` before AND after PROGRAMMING (no switch); all-bus-listen saw no `0x7a9` on any real bus. The "bus 129" line = `0x80 | 1` = the panda's TX-echo of our own two sends (0x7a1 + 0x7df), not a reroute (a small all-bus-listen cosmetic to filter). On the software path the EPS **refuses** programming rather than rerouting off-bus, which weakens the pin-swap theory — a repin can't fix a refusal.
  - **EPS identity (bus 1):** app_sw_id 8965F1208000, ecu_serial 8965012N50E12H030731, active_session 0x03; only 0xF181/0xF186/0xF18C answer, rest → 0x31.
  - **Operational:** EPS reachable 10:20–10:21 (read-mem/ident/prog on bus 1), unreachable 10:22 (dataflash-diag + reset-probe both "no response on 0/1/2"). It drops out of the diagnostic-responsive state after a probe burst — needs re-enter-NRtD / panda power-cycle between runs; the 10:22 screens are inconclusive from state, not content. No security key was ever sent (0x01 seed failed at 0x7e), so no lockout.

  **Concept (seed ≠ key):** UDS SecurityAccess (0x27) uses odd sub-functions = requestSeed, next even = sendKey; each pair is a separate level with its own secret. The **seed is a non-secret challenge nonce** (regenerates each request, fine to share) — NOT the SecOC key. The SecOC key is data in DataFlash that passing security lets you read. All the difficulty is the seed→key formula `f()`; without it, SEND_KEY fails with NRC 0x35.

  **Reframed path (cheapest first):** (1) **SEND_KEY at 0x03/0x04 using Willem's existing secret** — one-line probe change reusing the AES math; accepted → Corolla shares the secret, security opens in extended; NRC 0x35 → different secret. (2) Check whether the 0x03 seed→key algorithm is already published (Toyota tuner / locksmith / key-programmer circles, 8965F12). (3) Fallback: bench `8965F1208000` firmware dump (glitch, 3b1b.eth's Sienna path) — yields the 0x03 formula + the key's memory address (for 0x23) + probably the programming secret, all at once. No brute-force (128-bit) and no known seed/key pairs to analyze, so those are the only paths. **Not built:** the 0x03/0x04 SEND_KEY test (awaiting go).

- **2026-07-24 (Corolla `span` — afternoon: 0x03 seed isolated as real; Willem key rejected NRC 0x35)** — Built and pushed the two 0x03 follow-up probes (commit `10fc6bb374`, correctness fix `ced38e5498`); Spanconstant ran the full probe set in-car 15:13–15:16 (same panda `DEV-e1b3ecb5-DEBUG`, EPS bus 1). Read at the time as settling the security question; **superseded the same evening — the send-key went out at level 0x03/0x04, not the 0x01/0x02 level Willem's secret belongs to, so it does not test level-0x01 sharing. See the 07-24 evening entry.**

  **Shipped (`10fc6bb374` + `ced38e5498`):**
  - `level3_probe.py` + `level3-probe.html` + `/api/level3-probe` — 0x03-seed isolation. Requests seed 0x03 as the FIRST security op from a fresh connect, then adds one primer at a time (prior 0x01 request, prior PROGRAMMING poke), recording every session transition so a swallowed DEFAULT reset can't hide. Seed-only, no key sent, safe to re-run. Classifies reproduced / conditional / no_seed.
  - `sendkey_probe.py` + `sendkey-probe.html` + `/api/sendkey-probe` — request seed 0x03, compute the Willem key (`AES_decrypt(SECRET, 0^16)` → `AES_encrypt(that, seed)`, identical to `extractor.hack`), send at 0x04, one shot. Tap-to-run page + lockout warning (not auto-run); on unlock, retries 0x23 at the key region. `ced38e5498` reserves the "different secret" claim for NRC 0x35 alone; other NRCs (0x22/0x24/…) → status `rejected` with the raw code, so a handshake error isn't misread toward the firmware dump.
  - Both `panda_lock`-serialized, `is_agnos`-gated with off-device mocks, under Not Ready To Drive. Off-device tests: endpoints POST+poll, pages 200, gate raises NotAGNOSError, 409 both ways, plus a positive-control (broken mock → suite fails at the predicted assertion → reverted green).

  **In-car findings (15:13–15:16):**
  - **Send-key 0x03/0x04 → NRC 0x35 invalid key — the headline.** Seed `87734f83613e87a68ecccba880e3f122`, key `36c20b4723967a953d1cb888625fa0eb` (Willem's math), sent once → `0x35`. The Sienna secret+transform does not produce the key this EPS expects. One attempt, no lockout (0x35, not 0x36/0x37). Per Code's bundle caveat, a 0x35 rules out "reuse Willem's math as-is" but does NOT isolate whether the secret *value* or the two-stage *transform* differs — only a firmware dump separates those. The probe prints "different secret" (the common case); the precise statement is "the Sienna secret and/or transform don't match." **Corrected the same evening — the level was wrong (see the 07-24 evening entry): Willem's secret is the level 0x01/0x02 secret, and this probe sent it at 0x03/0x04, so the 0x35 does not test level-0x01 sharing at all.**
  - **Level 0x03 isolation → reproduced.** Answers the side-effect concern. Clean connect: default accepted, extended accepted, seed 0x03 = `d976cb541b8423555a263f8c913eef64` as the *first* security op — no PROGRAMMING, no prior 0x01 ahead of it. Default-session variant → NRC 0x7f (0x03 needs extended). The 0x01-first and programming-poke variants both still return a 0x03 seed. The seed is the EPS's own input/output, not an artifact of the morning sweep's prior traffic.
  - **Nonce behavior (answers the open nonce question).** Within one session the seed is STABLE — `seed 0x03` and `seed 0x03 again` both `d976…`. Across sessions it REGENERATES — `d976…` / `63fabc805eb9c3a919808b8468b19503` (0x01-first) / `be839062642f1f617abc28237eb4ed33` (prog-poke) / `87734f…` (send-key), one per fresh default→extended. A per-session nonce (standard anti-grinding), not per-request, not static. **Message bug:** level3's summary prints "seeds differ each request" (driven by the cross-test set having >1 value) while the in-session data shows the same seed twice; accurate phrasing is "differ each session, stable within a session." One-line `note` fix in `level3_probe.py`, unbuilt.
  - **prog-probe reproduced the morning result:** 0x03 → seed `0438cd94bc7221c63c070554a2a7658e`; 0x01 → 0x7e; 0x05/07/09/0b → 0x12; did-it-take before/after both `0x03` (session unchanged — refused); all-bus-listen no `0x7a9` (bus 0/2 = 22 IDs, bus 1 = 2, bus 129 = TX echo); safety-system 0x04 → NRC 0x12. Programming refused, not rerouted — again.
  - **0x23 unchanged:** key region `0xFF206E14` / dataflash base `0xFF200000` / ram `0xFEBF0000` all NRC 0x31 (out-of-range) in extended, 0x7f in default. Direct read still refused at the Sienna addresses.
  - **Reset-probe reached the EPS this run and read session `0x01` after the reset** (morning it was unreachable at 10:22). The `ecu_reset` response timed out, but session `0x01` (default) is consistent with the EPS having rebooted to default; PROGRAMMING still timed out through the reset window. So programming refusal is not a warm-up/state artifact — refused even immediately after a reset.
  - **dump_diag** hit the same wall: session PROGRAMMING → MessageTimeoutError (traceback `dump_diag.py:190` → `uds.py:680` diagnostic_session_control → `uds.py:493` recv), 0 frames. As expected — it needs programming.
  - **EPS identity, fuller this run:** `app_sw_id` (0xF181) carries TWO strings — `8965F1208000` plus a second `8A31…` (calibration/software id) — captured on both ident-map and dump_diag; `ecu_serial` `8965012N50E12H030731`; active_session `0x03`; rest → NRC 0x31. CAN sniff (8 s): bus 0/2 = 22 IDs CAN-FD 64B (identical, harness-bridged), bus 1 quiet (2 IDs `0x45a`/`0x4e0`, no sync 0x0F — NRtD, EPS not signing).

  **What it settles** *(as read on the day — the last two sentences are superseded by the 07-24 evening entry)***:** 0x23 is out-of-range at the Sienna addresses, PROGRAMMING is refused even post-reset, and a Willem-derived key was rejected at 0x35. ~~The 0x03 seed→key formula is genuinely unknown and is NOT the Sienna's~~ — the 0x35 came from level 0x03/0x04, a different level pair from Willem's 0x01/0x02, so it says nothing about the Sienna secret at its own level. ~~Short of the formula being published, the bench dump is the remaining route~~ — the `0x85`/`0x28` pre-programming preamble had never been tried and is a free software move against the actual wall, so the hardware path is not the only one left.

  **Post-0x35 options (risk-ordered, lockout-aware):** the obvious software shot (Willem's key) is spent, not the software space. Ordered by cost to the car:
  - **Zero-lockout, do first.** (a) **Community intel** — ask Willem, yc (his master-key work may touch this scheme), 3b1b.eth (did the Sienna glitch dump, knows the family's silicon), and locksmith / AKL key-programmer tools where newer-Toyota 0x27 algorithms circulate. Free, highest-value, could end the problem before any hardware. (b) **Pin the exact RH850 MCU** — the real part (bench-unit board photo / FCC teardown / 8965F12 parts catalog), not the "TSS3 / CAN-FD" guess. Gates everything downstream: the memory map for the 0x23 sweep, whether 3b1b.eth's glitch transfers, whether a known seed→key already exists for that silicon. (c) **0x23 read sweep, no security** — 0x23 is *out-of-range* (0x31), not *denied* (0x33), at the Sienna addresses, so it isn't security-gated there; sweep the pinned MCU's DataFlash/code bases (needs (b) to be targeted, not a blind 32-bit walk) to try reading the key region directly, and map which regions are invalid vs denied for a later read. (d) **Seed-side `data_record` variants** — a different REQUEST_SEED (0x03) `data_record` sends no key and touches no counter; free, folds into the read session.
  - **Lockout-exposed — deliberate gamble, not a sweep.** **Key-variant SEND_KEY** (different transform / byte-order / candidate secret). Each wrong key is a *counted* SecurityAccess attempt returning 0x35; past the threshold the EPS returns 0x36/0x37, and if that counter is NVRAM-backed it could **persistently lock SecurityAccess on Spanconstant's car** — the one failure a power cycle won't undo. We've already spent one counted attempt (the 15:16 send-key) with no recovery re-test, so a key-variant run starts at attempt 2 of an unknown, untested budget; the Sienna gives no cover (it takes Willem's key first try, so wrong-key behavior is unexercised). At most one or two hand-picked guesses from fresh NRtD, halting on the first 0x36, and only after the lockout policy is characterized or the risk is explicitly accepted. **Correction to the earlier "two cheap sweeps" framing:** only the seed-side is cheap; the key-side is a bounded risk to the car, and sits after the free moves.
  - **Parallel route (not a probe) — rekey / master-key.** Write a *known* SecOC key past the pairing master and skip f() entirely. Different door: SecOC is a shared key, so rekeying implicates whatever verifies the EPS too, and per the no-Ghidra read the rekey path sits in the **ICU-S** — the EPS MCU's own on-die security block (same RH850 as the main core, not a separate module), which is why a main-core read or patch can't reach it. *(Corrected 07-24 evening: this line previously said "a different ECU's security," which implies a separate module.)* yc-dependent, near-term unlikely (the Corolla key table isn't at the Sienna RAM addresses), but a line worth keeping open rather than treating the formula as the only door.
  - **Hardware fallback — bench `8965F1208000` glitch dump,** once the free moves dry up. It reads *program flash* (code), where f() lives; the DataFlash/RAM dump the exploit produces is *data* (the key), so no exploit dump can ever contain f(). Risks: the glitch may not transfer to this MCU, the secret may sit in OTP / fuses / a crypto block outside the flash image (algorithm without key material), and it may be per-unit rather than per-model (the Sienna's is one fleet-wide constant — precedent favors per-model, unverified here).

- **2026-07-24 (Corolla `span` — evening: the 0x35 tested the wrong security level; preamble probe shipped)** — No new in-car data. A re-read of what was already in hand overturned the day's framing, and one commit (`8efbcfb867`) followed from it. Reviewed with the second Code instance, which accepted the correction and re-ordered the plan around it.

  **The correction — Willem's secret was sent at the wrong level.** Willem's Sienna path uses security level **0x01/0x02**: `extractor.py:184,195`, `dump_dataflash.py:180,183`, and `dump_diag.py:201,216` all call `ACCESS_TYPE.REQUEST_SEED`/`SEND_KEY`, which are `1` and `2` in `opendbc/car/uds.py:56-57`. `sendkey_probe.py:32-33,154-164` derived a key from that secret and sent it at **0x04**, against a **0x03** seed. Each UDS level pair carries its own secret, so NRC 0x35 there is the expected result whether or not the Corolla shares the Sienna's level-0x01 secret. The Corolla's level 0x01 *exists* — NRC 0x7e (wrong session), while 0x05/07/09/0b return 0x12 (absent) — and sits behind PROGRAMMING. **Willem's secret is still untested at the level it belongs to.** The 0x35 is consistent with "different secret," not evidence for it.

  **What that moves.** The blocker never left PROGRAMMING; the 07-24 morning reframing ("the blocker moves from how to enter programming to what's the 0x03 formula") was premature, and the 0x03-formula hunt comes off the board as the primary target. Level 0x03 being handed out in *extended* argues it is a lesser privilege (extended-session security on these ECUs typically gates calibration/variant writes, not flash reads), so recovering f() for 0x03 could open a surface that doesn't contain the key. Under the correction the RE target for any future dump is specifically the **0x01/0x02** routine — the function Willem's secret feeds.

  **Shipped (`8efbcfb867`) — `preamble_probe.py` + `preamble-probe.html` + `/api/preamble-probe`.** Every prior probe entered PROGRAMMING cold; OEM reflash tools quiesce the bus first with ControlDTCSetting OFF (`0x85`) and CommunicationControl disable-normal-tx (`0x28`), and prog_probe's five sequences (`prog_probe.py:137-180`) never sent either. One ~30–60 s run, ordered so a mid-run drop-out still leaves the valuable data: **(A)** bus sweep + identity; **(B)** lock read — a bare 0x03 REQUEST_SEED as the first security op, which is the first measurement of whether the 15:16 send-key left a persistent lock (0x36/0x37 stops the run), plus the 0x01 baseline; **(C)** Willem's exploit surface by refusal code only — DIDs 0x201/0x202/0x203, RoutineControl results on 0x10f0, RequestDownload to RAM, and whether 0x85/0x28 are accepted (0x7f = exists and session-gated, 0x11 would mean the exploit path is absent on this EPS even with PROGRAMMING open); **(D)** five preamble entries — `0x85`; `0x85+0x28`; the same at a 6 s timeout; a raw `10 82` (suppressPosRsp) read back via 0xF186 instead of waiting; functional `0x28` to 0x7DF then physical `10 02` — each followed by a session read **and** a 0x01 REQUEST_SEED retry, since a 0x01 seed at any point makes Willem's secret testable at its own level and costs no counted attempt to detect; **(E)** a DTC snapshot diff, which separates an application-layer refusal (a fresh code naming the unmet condition) from a lower-layer drop (empty diff + bus silence → ISO-TP / addressing / gateway) — nothing so far distinguished those; **(F)** 0x23 retried at the three addresses, then a liveness read so a null tail is attributable. No key sent, no counter touched; `0x85` ordered before `0x28` throughout (DTC-off first suppresses the lost-message codes other ECUs set while EPS tx is disabled), both restored plus a DEFAULT session in a `finally`.

  **One button.** Not Ready To Drive went from eight buttons to one — read-mem, ident-map, prog-probe, level3-probe, dataflash-diag and reset-probe are all folded into the single run; READY has nothing left to do (`sniff_can.py:116-119` records frame counts and arb IDs, never payloads, so it cannot do the open signed-ID capture). All hidden via `span-hidden` rather than deleted, so the URLs still serve if a result reopens one. **sendkey-probe is retired outright** — re-running it spends another counted attempt at a level Willem's secret does not belong to. "Install a different fork/branch" is hidden too: it deletes `/data/continue.sh` and would drop the device to the comma installer mid-session. Off-device tests include a positive control (an extra visible button injected → suite failed at the predicted assertion → reverted green).

  **Offline oracle — the move that reshapes the community ask.** A key-variant sweep is worthless without a known (seed, key) pair for an 8965F12: with one, thousands of candidate transforms test offline at zero risk; without one, each variant costs a counted live attempt with no pre-filter. A pair is obtainable at zero risk by **sniffing a legitimate unlock** — Techstream, a dealer reflash, or a locksmith/AKL tool performing an EPS security transaction puts the seed *and* the key on the bus in plaintext, and the sub-function bytes say which level the tool used. A 0x03/0x04 capture gives that level's mapping directly; a 0x01/0x02 capture is the oracle to test Willem's secret **offline** before spending the single live attempt. The capture harness can be positive-controlled on Calvin's Sienna, where TSKM performs a legitimate level-0x01 unlock every extraction run and the expected pair is already known. That changes the ask from "does anyone have the algorithm" (rarely yes) to "can anyone capture a seed/key pair" (much easier yes).

  **ECU_MASTER_KEY delivery.** `extractor.py:315-323` already extracts *and* checksum-verifies slot 1 (`ECU_MASTER_KEY`) beside slot 4 (the SecOC key), but `hack()` returns only `key_4` (line 327) and `/api/extract` (`server.py`) ships only `secoc_key` — so on `tskm` the master key reaches the **server's stdout**, not the phone page. Calvin's Sienna-vs-RAV4 cross-part comparison needs an SSH look at the server log during a run, or a one-line surface change; on the RayLib branches (through `tskm-0.11.0`) the console was the UI, so it is already visible there. Calvin's classification (his call, recorded): the master key is **not a secret** — any owner of a 2021-23 Sienna or RAV4 Prime obtains it from a public tool on their own car; the *SecOC key* is the secret. A shipped `sha256(key_1)` comparison constant was considered and **kept out**: no Corolla-side master key can exist while PROGRAMMING is refused, and for the 21-23 family the SecOC key is already extracted directly, so fleet-sharing changes nothing actionable today.

- **2026-07-24 (Corolla `span` — 17:33 in-car: the preamble refused, and the wall turns out to be a service class)** — Spanconstant ran `preamble_probe` (`8efbcfb867`). EPS answering at the end of the run, so every result is attributable rather than a drop-out artifact.

  **Answers.** *No persistent lock* — seed 0x03 `ce7b6b2c79b21c01e9361fd4e390bcfc` came back as the first security op, so the 15:16 send-key left nothing behind and the attempt budget is not visibly spent. The 0x01 baseline in extended was NRC 0x7e as before. Identity: `app_sw_id` two strings (`8965F1208000` + `8A3111213000`), `ecu_serial` `8965012N50E12H030731`, `active_session` `0x01` at connect.

  **Willem's upload path does not exist on this EPS.** Routine `0x10f0` → NRC **0x31** from a service that demonstrably answers, so the routine identifier is not recognised; DIDs 0x201/0x202/0x203 → NRC 0x31. Even with PROGRAMMING open, his sequence would not run unmodified here.

  **The reframe.** `0x85`, `0x28`, `0x34` and `0x10 02` all produced **MessageTimeoutError** — total silence — while `0x22`, `0x23`, `0x27`, `0x31`, `0x19` and `0x10 01/03` all answered, with data or a precise NRC. This EPS says "no" freely and specifically, so silence across one coherent class is a different phenomenon from refusal. That moved the question from "why is PROGRAMMING refused" to "what defines the silent class," which is what the 07-25 sweep was built to answer.

  **Two defects in the probe, both mine.** (a) The preamble was never delivered — `0x85`/`0x28` are themselves in the silent class, so the hypothesis was untested rather than refuted. (b) The stacked 3 s timeouts burned the S3 window: in variants 2, 3 and 5 PROGRAMMING went out 6+ s after the EXTENDED request with no tester-present, i.e. from a lapsed session. The run's own data shows it — the fresh-extended baseline answered `seed 0x01: NRC 0x7e` (sub-function wrong for this session) while every post-variant retry answered `NRC 0x7f` (service not in this session), which is the default-session response. Variant 1 was inside S3 and is valid, but with `0x85` dropped it reduces to prog_probe's existing patient test.

  **One unresolved lead.** The suppressPosRsp variant reported `session after: NRC 0x7e`, and a 0x22 read cannot legitimately produce 0x7e — that looks like a late `7F 10 7E` answering the raw `10 82` and being picked up by the following request. If the EPS negatively acknowledges `10 82` while staying silent on `10 02`, it is the only non-silent programming response ever seen, and it says "wrong session" rather than "service absent." Inference from a misattributed frame, not a result.

  DTC diff empty (0 codes before and after). 0x23 unchanged at all three addresses.

- **2026-07-25 (Corolla `span` — the exhaustive sweep: two undocumented services, and the repin theory dies)** — Shipped `b9db236acc` (`sweep_uds.py` + `capture_ready.py` + two pages) after Calvin pushed back three times on scope. The correction that produced it: **"test everything" means the full value space** — every service 0x00–0xFF, every sub-function 0x00–0xFF, not the subset that is standard, documented, meaningful, or safe. Safety constrains *how* a value is sent (bare service byte, DEFAULT before EXTENDED, restore + liveness after each block), never *whether* it is sent, and coverage is bounded by a **deadline** with a recorded frontier and a resumable on-disk log, never by a skip list. The root cause of the repeated narrowing was an unexamined assumption of mine: every probe had been designed to fit a one-tap sixty-second page, and that constraint forces curation and then rationalises it.

  Spanconstant ran it across several Not Ready to Drive sessions (~10:15) plus a READY attempt (~10:23). Calibrated round trip 14 ms → 140 ms silence threshold. 3,508 records saved to `/cache/tsk/uds-sweep/uds_sweep.ndjson`.

  **Two undocumented services answer: `0xAB` and `0xBA`.** Neither is in ISO 14229. This is the single result that justifies sweeping the byte space, and no curated list would have contained them. Their sub-functions are **unswept** — the run stopped before reaching them.

  **The read-vs-write framing was wrong.** Answering (13): `0x10 0x14 0x19 0x22 0x23 0x27 0x2e 0x31 0x36 0x37 0x3e 0xab 0xba` — including `0x2E` WriteDataByIdentifier, `0x14` ClearDiagnosticInformation, and `0x36`/`0x37` TransferData/TransferExit, which are writes and reprogramming services. The silent set is exactly four: **`0x11` reset, `0x28` comm control, `0x34` request download, `0x85` DTC setting**. The 132-entry silent list is mostly `0x40–0x7F` and `0xC0–0xFF`, which are the *response* SID ranges (request + 0x40), not request identifiers — nothing should answer there. The real request space is `0x00–0x3F` and `0x80–0xBF`.

  **The sharpest lead: `0x34` is silent while `0x36` and `0x37` answer NRC 0x7f.** Same download sequence. TransferData and TransferExit exist and say "wrong session"; RequestDownload — the gate to the entire upload path — says nothing.

  **The repin theory dies.** 13 responders on bus 1: `0x700 0x724 0x747 0x780 0x792 0x7a1 0x7b0 0x7b3 0x7c0 0x7c4 0x7d0 0x7d2 0x7f1`. Twelve go silent on all four silent services; **`0x7f1` returns a proper NRC to all four.** A path-level filter cannot drop those service IDs for twelve destinations and pass them for the thirteenth on the same wire, so the drop is per-ECU behaviour, not a filter on the path — **rewiring would not fix it.** This reverses the 07-25 morning lean, which had itself reversed the 07-24 morning lean. (`0x7f1` may be the gateway; either way the conclusion about the path is unchanged.)

  **The method's own negative control passed.** The recheck stage re-sent all 132 silent services at 1400 ms — ten times the calibrated timeout — and the silent count stayed 132. The silences are not an artefact of too short a window.

  **READY capture: almost certainly not in READY.** 75,192 frames across **46 IDs, zero `0x0F` sync frames, zero SecOC candidates**. On 07-23 the same car in READY showed 147 IDs on bus 1 plus sync; 46 ≈ 22 + 22 + 2 is the Not-Ready-to-Drive profile exactly. The pass needs a mode guard that refuses to start until it sees READY traffic, and the signed-ID capture remains open.

  **A wrong summary line, mine.** "6 of 139 answered in READY that did not in Not Ready to Drive" counts the condition-NRC bucket (`0x23 0x27 0x2e 0x36 0x37 0xba`), which answered in *both* modes and was re-asked because of its NRC, not because it was silent. Actual silent→answering count: **zero**.

  **A data-integrity defect, mine.** Liveness is checked only between sub-function blocks. After the check failed, the run continued for roughly 900 more requests (Willem DIDs, manufacturer DIDs, the address sweep, the recheck), every one recording a "silent" that cannot be vouched for. What survives: the **service sweep is sound**, because it is bracketed by real answers — `0x10` answered near the start of the block and `0xba` near the end, so the EPS was alive throughout; and the `0x7f1` cross-ECU result rests on responses received, which a dead bus cannot manufacture. The DID sweeps are the untrustworthy part.

  **Retraction: `0x14` did not kill the EPS.** The screenshots show `subfunctions/14: 0 sent — EPS stopped answering after this service` on a run where **zero requests were sent** (all already recorded from a prior run), so the EPS was gone before `0x14` was attempted. Later in that same run the address sweep got a response from `0x7a1` — the EPS itself — so it dropped out and recovered rather than dying. That matches the long-standing drop-out behaviour (open item 16).

  **Spanconstant: the car powers itself off after some time in Not Ready to Drive.** A separate, real constraint on top of the drop-outs, and not under his control — which makes the run budget the car's timer, not the 12V. Both produce silence; they are not the same thing.

  **Safety audit of what was actually sent.** The sub-function sweep runs services in numeric order and stopped at `0x14`, so `0x19` onward never received a payload: no write via `0x2E`, no security attempt via `0x27` (no counted failures), no routine started via `0x31`, no transfer via `0x36`/`0x37`. `0x11`, `0x28` and `0x85` are in the silent class and never took effect. The only plausibly persistent change is cleared fault codes, if any of the 256 malformed `[0x14, sf]` requests were accepted — `0x14` expects a 3-byte DTC group, so a 2-byte request should have been rejected on length, and the NDJSON on the device records every one of those responses and settles it without another in-car run.

**Current state.** Active work is on `tskm`: the Sienna production pipeline, validated in-car, car-agnostic, no EPS version guard.

**The Corolla investigation on `span` is closed** (2026-07-26). Every probe set is shipped, pushed, and run in-car (`d6526664f2`, `10fc6bb374` + `ced38e5498`, `8efbcfb867`, `b9db236acc`), and the branch is published for public reading. Everything below about the Corolla is the record handed off with it, not an active queue.

Corolla (EPS `8965F1208000`, bus 1, all buses CAN-FD), as of the 07-25 sweep:

- **The wall is a four-service silent class, not PROGRAMMING.** `0x11`, `0x28`, `0x34`, `0x85` produce total silence; `0x10 0x14 0x19 0x22 0x23 0x27 0x2e 0x31 0x36 0x37 0x3e 0xab 0xba` all answer, with data or a precise NRC. Writes and reprogramming services (`0x2E`, `0x14`, `0x36`, `0x37`) are on the answering side, so it is not a read/write split.
- **`0xAB` and `0xBA` answer and are outside ISO 14229.** Sub-functions unswept. The most interesting unexplored surface on the car.
- **`0x34` is silent while `0x36`/`0x37` answer NRC 0x7f.** One download sequence, split behaviour, and `0x34` is the gate.
- **A repin would not help.** `0x7f1` answers all four silent services on the same bus where twelve other responders go silent, so the drop is per-ECU, not a path filter. (Reverses both earlier leans.)
- **Security is not locked** — a clean 0x03 seed after the 15:16 send-key. Level 0x01 exists (NRC 0x7e), sits behind PROGRAMMING, and **Willem's secret is still untested at its own level**; the 0x35 was spent at 0x03/0x04.
- **Willem's upload path is absent regardless**: routine `0x10f0` and DIDs 0x201/0x202/0x203 all NRC 0x31.
- 0x23 read-memory out-of-range at the Sienna addresses, unchanged across every run and both modes.

The sweep is **partial and resumable** — the sub-function layer stopped at `0x14`, so `0x19` through `0xBA` are unswept, and roughly 900 records written after a failed liveness check are untrustworthy. Two operational constraints shape every future run: the EPS drops out under load and recovers, and Spanconstant's car powers itself off after some time in Not Ready to Drive.

The order left for whoever picks it up: fix the sweep's liveness/budget/order defects and resume it at `0xAB`/`0xBA` → pull the NDJSON off the device (it already holds the answers to several open questions) → re-run the READY capture with a mode guard → capture-a-pair from a sniffed legitimate unlock → pin the exact RH850 MCU → the 0x23 read sweep it enables → and only if PROGRAMMING opens, the single live 0x01/0x02 attempt with Willem's secret. Key-variant guessing and the bench glitch dump both sit below all of that.

**Open items.**
*Sienna / production:* (1) the host/panda-side cause of the cold-run frame drop is unresolved (EPS ruled out — a state machine has no warm-up).

*Closed by Calvin, 2026-07-26:* the cold-run frame drop is **universal, not car-specific** — his classification, from the absence of any user report of it behaving differently. `ECU_MASTER_KEY` stays **server-stdout only**; surfacing it in `/api/extract` is not wanted. On the restart-vs-keep-powered wording: the `_finalize()` comment at `dump_dataflash.py:88-91` was corrected to the keep-the-comma-powered remedy, and **the three user-facing messages keep "restart the car into Not Ready To Drive mode and dump again" by decision** — `dump_dataflash.py:100-101` (`key_missed`), `:116-118` (`partial`), `server.py:369-370` (after-restart partial).
*Corolla (`span`) — **closed 2026-07-26; kept as the handoff record, not an active queue.** Re-ordered after the level correction (detail in the 07-24 PM options block, as amended by the 07-24 evening entry):* the 15:16 NRC 0x35 is answered only for level 0x03/0x04; **level-0x01 sharing is untested and is now the open question**, blocked behind PROGRAMMING. **Sweep defects to fix before the next run (all mine, all in `sweep_uds.py` / `capture_ready.py`) —** (4a) liveness is checked only between sub-function blocks, so ~900 records were written after the EPS stopped answering — add a liveness marker every ~32 requests and stop on death, and on analysis discard silents after the last good marker; (4b) the budget assumed the 12V was the limit, but the car's auto-off is shorter and not controllable — drop to ~2 minutes per run and lean on resume; (4c) sweep sub-functions in interest order (`0xAB`/`0xBA` first) rather than numeric, and move `0x14` last with a valid 3-byte DTC group so it cannot clear anything; (4d) the READY pass needs a mode guard (refuse to start until bus 1 shows READY traffic / sync `0x0F`); (4e) the mode-diff summary counts the condition-NRC bucket as "answered in READY that did not in NRtD" — wrong bucket, actual count zero; (4f) add a download endpoint for `uds_sweep.ndjson` — 3,508 records are on the device and answer what `0xAB`/`0xBA` returned, whether the `0x14` clears were rejected on length, and where the liveness line falls, without another in-car run. **Sweep continuation —** (4g) resume the sub-function layer: `0x19 0x22 0x23 0x27 0x2e 0x31 0x36 0x37 0x3e 0xab 0xba` are entirely unswept; (4h) chase `0x34` silent vs `0x36`/`0x37` answering, the sharpest split found; (4i) identify `0x7f1`, the one responder that answers all four silent services. **Free / zero-lockout —** (5) capture-a-pair: ask Willem, yc (master-key work), 3b1b.eth, and locksmith/AKL circles for a *sniffed seed+key pair* off an 8965F12 rather than for the algorithm — a legitimate unlock puts both on the bus in plaintext and is a zero-risk offline oracle; positive-control the capture on Calvin's Sienna, where the pair is already known; (6) pin the exact RH850 MCU (board photo / FCC / 8965F12 catalog) — gates the sweep + the hardware decision; (7) 0x23 read sweep, no security, over the pinned MCU's memory bases (0x23 is out-of-range 0x31, not denied 0x33, at the Sienna addresses); (8) seed-side `data_record` variants on REQUEST_SEED (no key, no counter). **Only if PROGRAMMING opens —** (9) the **single** live SEND_KEY with Willem's secret at **0x01/0x02** — the correct level, and the shot that could simply work; de-risk it first with an offline oracle if one exists by then. **Lockout-exposed — gamble only —** (10) key-variant SEND_KEY: each wrong key is a counted attempt (≈1 already spent at 15:16, recovery untested; NVRAM persistence unknown → could permanently lock SecurityAccess); at most 1–2 hand-picked guesses from fresh NRtD, halt on the first 0x36 — not a sweep, and pointless without an oracle. **Parallel route —** (11) rekey/master-key: write a known SecOC key past the pairing master, skip f(); yc-dependent, via the EPS's own on-die ICU-S, near-term unlikely. **Hardware fallback —** (12) bench `8965F1208000` glitch dump (program flash = code, where f() lives; the RE target is the **0x01/0x02** routine; risks: glitch transfer, secret in OTP, per-unit vs per-model). **Loose ends —** (13) PROGRAMMING as a CAN-FD-framed request (weak: DEFAULT/EXTENDED already answer classic framing on bus 1, and a 2-byte request is a single frame either way); (14) fix level3's "seeds differ each request" → "differ each session, stable within a session" (one-line `note` change); (15) filter the `0x80` TX-echo bus (129) from all-bus-listen; (16) EPS drops out between probe bursts *and mid-run under load*, recovering on its own (07-25: reported dead by the liveness check, then answered the address sweep at `0x7a1` later in the same run) — distinct from Spanconstant's separate report that the car powers itself off after some time in Not Ready to Drive, which is normal power management and bounds every run; (17) copy-as-text is shipped on the sweep and capture pages (textarea + select-all, since the Clipboard API needs a secure context and this is plain http) — the older probe pages still need 3-4 scrolling screenshots; (18) surface `ECU_MASTER_KEY` in `/api/extract` — **dropped 2026-07-26, not wanted;** (19) repoint the production dump to bus 1; the Corolla's SecOC-signed IDs are still uncaptured — `capture_ready.py` is built for it (full payload + an offline MAC-tail detector) but the 07-25 attempt ran in the Not-Ready profile (46 IDs, no sync `0x0F`) so it produced nothing; driving needs a repin or fork bus-patch — separate from extraction, and note the 07-25 finding that a repin would not help *extraction*; **(20)** `preamble_probe`'s functional-`0x28` variant (variant 5, `0x28`→`0x7DF`) silences normal TX **bus-wide**, but the `finally` re-enables only the EPS (physical `mk(eps_bus)`, `preamble_probe.py:462`) — other bus-1 ECUs it quieted stay TX-disabled until a power cycle (low impact: NRtD + the power-cycle-between-runs workflow self-heals it; a functional `ENABLE_RX_ENABLE_TX`→`0x7DF` in the `finally` would match the action).
*`span` state:* all probe sets are committed and pushed (`10fc6bb374`, `ced38e5498`, `8efbcfb867`, `b9db236acc`); the branch is final.

**Standing rule from 07-25 — "test everything" means the full value space.** Everything is the complete enumeration — every service 0x00–0xFF, every sub-function 0x00–0xFF, every DID 0x0000–0xFFFF — not the subset that is standard, documented, meaningful, or safe. Safety constrains *how* a value is sent, never *whether* it is sent: ask in a form that must be rejected (bare service byte, nonexistent DID, out-of-range address, invalid sub-function) and the ECU answers without acting, because it decides whether to respond before it decides whether to act. Runtime is a scheduling problem, not a scoping one — if full coverage takes hours, build a resumable deadline-bounded job, not a shorter list. Any value not sent must appear in the output as an explicit reasoned omission; a shorter list is never the silent answer. The failure mode this replaces: designing every probe to fit a one-tap sixty-second page, which forces curation and then rationalises the subset as "the meaningful ones."
